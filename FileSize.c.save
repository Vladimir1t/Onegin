#include "FileSize.h"

int FileSize (FILE* const fPointer, int const startPosition)
{
    assert (fPointer != NULL);
    size_t len = 0;
    fseek (fPointer, , SEEK_END);
    len = (size_t) ftell (fPointer);

    rewind (fPointer);

    return len;
}
